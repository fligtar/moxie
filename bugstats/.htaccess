RewriteEngine On

# Main rewrite for projects and reports to work
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(\w*)(\/(.*?))?(\/(.*?))?$ project.php?project=$1&report=$3&params=$5